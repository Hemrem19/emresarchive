[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x", "openssl", "python3", "gnumake", "gcc"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = [
  "pnpm run db:generate"
]

[start]
cmd = "pnpm run start"

