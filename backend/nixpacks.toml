[phases.setup]
nixPkgs = ["nodejs_22", "pnpm", "openssl", "python3", "gnumake", "gcc"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install"]

[phases.build]
cmds = [
  "pnpm run db:generate"
]

[start]
cmd = "pnpm run start"

