[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x", "openssl", "python3", "gnumake", "gcc", "g++"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run db:generate"
]

[start]
cmd = "npm run start"

